import React from 'react';
import Navigation from '../components/Navigation';

const Recherche = () => {
    return (
        <div className="recherche">
            <Navigation/>
            Hello depuis Recherche
        </div>
    );
};

export default Recherche;